/**
 * Another way of export called "export default", it's used to export only one thing from a file
 * In this example we're exporting the function subtract, if there were another functions those will not be exported
 *
 */

export default function subtract(x,y) {return x - y;}

